<h1>Current Dog:</h1>
<div>
  Dog Name: <%= @dog.name %><br>
  Breed: <%= @dog.breed %><br>
  Age: <%= @dog.age %><br>
  <h3>Owners:</h3>
  <ul>
  <% @dog.employees.each do |emp| %>
  <li><%= emp.first_name + " " + emp.last_name %></li>
  <% end %>
  </ul>
</div>
